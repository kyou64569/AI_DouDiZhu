# Node 代理监听端口（开发期 Vite 会把 /api 反代到这里）
PORT=8787

# 转发到上游 LLM 服务的默认超时（毫秒）
LLM_TIMEOUT_MS=15000

# 日志级别：debug | info | warn | error
LOG_LEVEL=info

# 注意：本文件仅为样例，禁止在此填写任何真实 API Key。
# 用户的 API Key 由前端页面录入，保存在浏览器 localStorage，不经过服务端落盘。
